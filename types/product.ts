export interface product {
  name: string;
  price: string;
  description: string;
  url: string;
}

export interface summary {
  name: string;
  price: string;
  description: string;
  url: string;
}
